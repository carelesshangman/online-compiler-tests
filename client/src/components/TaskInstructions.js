import React from 'react';

const TaskInstructions = () => {
    return (
        <div className="task-instruction">
            {/* Add your task instructions here */}
            <p>Write a function to calculate the factorial of a number.</p>
            <p>The factorial of a non-negative integer is the product of all positive integers less than or equal to the number.</p>
            <p>For example, the factorial of 5 (denoted by 5!) is 120.</p>
        </div>
    );
};

export default TaskInstructions;
